using UnityEngine;

namespace BaaroForce.Keywords
{
    public class Root : Keyword
    {
        public Root()
            : base("Root", "Prevents the target from moving for the duration", new Color(0.35f, 0.55f, 0.25f))
        {
        }
    }
}
