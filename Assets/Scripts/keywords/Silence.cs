using UnityEngine;

namespace BaaroForce.Keywords
{
    public class Silence : Keyword
    {
        public Silence()
            : base("Silence", "Prevents the target from casting spells for the duration", new Color(0.55f, 0.35f, 0.75f))
        {
        }
    }
}
